# 🏢 پنل ادمین کامل - سیستم درخواست تغییرات

## 📋 فهرست مطالب

- [ویژگی‌های اصلی](#ویژگی‌های-اصلی)
- [نصب و راه‌اندازی](#نصب-و-راه‌اندازی)
- [پشتیبانی از نایت مود](#پشتیبانی-از-نایت-مود)
- [پشتیبانی از RTL](#پشتیبانی-از-rtl)
- [فیلترها و جستجو](#فیلترها-و-جستجو)
- [ویجت‌های سفارشی](#ویجت‌های-سفارشی)
- [اکشن‌های گروهی](#اکشن‌های-گروهی)
- [قالب‌های سفارشی](#قالب‌های-سفارشی)
- [کدهای CSS و JavaScript](#کدهای-css-و-javascript)
- [تنظیمات پیشرفته](#تنظیمات-پیشرفته)

## 🚀 ویژگی‌های اصلی

### ✅ پنل ادمین کامل و بهینه
- **مدیریت کامل تمام مدل‌ها**: تمام 25+ مدل موجود در سیستم
- **فیلترهای پیشرفته**: فیلترهای سفارشی برای جستجوی آسان
- **جستجوی هوشمند**: جستجو در تمام فیلدهای مرتبط
- **صفحه‌بندی بهینه**: نمایش 25 رکورد در هر صفحه
- **اکشن‌های گروهی**: عملیات روی چندین رکورد همزمان

### 🌙 پشتیبانی کامل از نایت مود
- **تغییر تم یک‌کلیکی**: دکمه تغییر تم در گوشه صفحه
- **ذخیره تنظیمات**: انتخاب تم در localStorage
- **انیمیشن‌های نرم**: انتقال نرم بین تم‌ها
- **رنگ‌های بهینه**: رنگ‌های مخصوص تم تاریک و روشن

### 🔄 پشتیبانی کامل از RTL
- **جهت متن راست به چپ**: تمام عناصر با جهت RTL
- **فونت فارسی**: استفاده از فونت Vazir
- **تاریخ و زمان فارسی**: ورودی تاریخ و زمان به شمسی
- **رابط کاربری فارسی**: تمام متن‌ها و پیام‌ها به فارسی

### 🎨 طراحی مدرن و زیبا
- **گرادیانت‌های زیبا**: استفاده از گرادیانت در دکمه‌ها و هدرها
- **انیمیشن‌های تعاملی**: انیمیشن‌های hover و focus
- **آیکون‌های emoji**: استفاده از آیکون‌های مناسب
- **رنگ‌بندی هوشمند**: رنگ‌های مناسب برای وضعیت‌ها

## 🛠 نصب و راه‌اندازی

### 1. فایل‌های ایجاد شده

```
ConfigurationChangeRequest/
├── admin.py                 # تنظیمات اصلی ادمین
├── widgets.py              # ویجت‌های سفارشی
├── context_processors.py   # پردازنده‌های context
└── admin_filters.py        # فیلترهای سفارشی

templates/admin/
├── base_site.html          # قالب اصلی ادمین
├── change_list.html        # قالب لیست رکوردها
├── change_form.html        # قالب فرم‌ها
└── index.html              # قالب صفحه اصلی

static/admin/
├── css/custom-admin.css    # استایل‌های سفارشی
└── js/custom-admin.js      # اسکریپت‌های سفارشی
```

### 2. تنظیمات Django

در فایل `settings.py`:

```python
# تنظیمات ادمین پنل سفارشی
ADMIN_SITE_HEADER = "🏢 مدیریت سیستم درخواست تغییرات"
ADMIN_SITE_TITLE = "مدیریت درخواست تغییرات"
ADMIN_INDEX_TITLE = "📊 پنل مدیریت"

# تنظیمات قالب‌های ادمین
TEMPLATES[0]['OPTIONS']['context_processors'].extend([
    'ConfigurationChangeRequest.context_processors.admin_context',
])
```

### 3. اجرای دستورات

```bash
# جمع‌آوری فایل‌های استاتیک
python manage.py collectstatic

# اجرای سرور
python manage.py runserver
```

## 🌙 پشتیبانی از نایت مود

### ویژگی‌ها
- **دکمه تغییر تم**: در گوشه بالا سمت چپ صفحه
- **ذخیره تنظیمات**: انتخاب کاربر در localStorage ذخیره می‌شود
- **انیمیشن نرم**: انتقال 0.3 ثانیه‌ای بین تم‌ها
- **رنگ‌های بهینه**: 
  - تم روشن: پس‌زمینه سفید، متن تیره
  - تم تاریک: پس‌زمینه تیره، متن روشن

### استفاده
```javascript
// تغییر تم به صورت برنامه‌نویسی
document.documentElement.setAttribute('data-theme', 'dark');
localStorage.setItem('admin-theme', 'dark');
```

## 🔄 پشتیبانی از RTL

### ویژگی‌ها
- **جهت متن**: تمام عناصر با `direction: rtl`
- **فونت فارسی**: فونت Vazir از Google Fonts
- **تاریخ فارسی**: ورودی تاریخ به فرمت 1403/01/01
- **زمان فارسی**: ورودی زمان به فرمت 14:30

### تنظیمات CSS
```css
body {
    direction: rtl;
    text-align: right;
    font-family: 'Vazir', 'Tahoma', sans-serif;
}
```

## 🔍 فیلترها و جستجو

### فیلترهای موجود

#### فیلترهای وضعیت
- 🟢 **فعال**: درخواست‌های در حال انجام
- 🟡 **در انتظار**: درخواست‌های پیش نویس
- ✅ **تکمیل شده**: درخواست‌های خاتمه یافته
- ❌ **ناموفق**: درخواست‌های ناموفق

#### فیلترهای زمانی
- 📅 **امروز**: درخواست‌های امروز
- 📅 **دیروز**: درخواست‌های دیروز
- 📅 **این هفته**: درخواست‌های هفته جاری
- 📅 **این ماه**: درخواست‌های ماه جاری

#### فیلترهای پیشرفته
- **اولویت**: استاندارد، فوری، اضطراری
- **تیم**: فیلتر بر اساس تیم درخواست کننده
- **سطح ریسک**: بالا، متوسط، پایین
- **جنسیت**: مرد، زن

### جستجوی هوشمند
- جستجو در کد سند، عنوان، نام درخواست کننده
- جستجو در فیلترها با دکمه جستجو
- ذخیره فیلترهای انتخاب شده

## 🎛 ویجت‌های سفارشی

### 1. ویجت تاریخ فارسی
```python
class PersianDateWidget(forms.TextInput):
    def __init__(self, attrs=None):
        attrs.update({
            'class': 'persian-date',
            'placeholder': '1403/01/01',
        })
```

### 2. ویجت زمان
```python
class TimeWidget(forms.TextInput):
    def __init__(self, attrs=None):
        attrs.update({
            'class': 'time-input',
            'placeholder': '14:30',
        })
```

### 3. ویجت انتخاب پیشرفته
```python
class EnhancedSelectWidget(forms.Select):
    def __init__(self, attrs=None, searchable=True):
        # قابلیت جستجو در گزینه‌ها
```

### 4. ویجت آپلود فایل
```python
class FileUploadWidget(forms.FileInput):
    # قابلیت drag & drop
    # نمایش پیش‌نمایش فایل
```

## ⚡ اکشن‌های گروهی

### اکشن‌های موجود
1. **📤 صادرات به CSV**: صادرات رکوردهای انتخاب شده
2. **✅ علامت‌گذاری به عنوان تکمیل شده**: تغییر وضعیت درخواست‌ها
3. **📢 ارسال اطلاعیه گروهی**: ارسال اطلاعیه به درخواست‌ها

### استفاده
```python
def export_to_csv(modeladmin, request, queryset):
    """صادرات به CSV"""
    response = HttpResponse(content_type='text/csv; charset=utf-8-sig')
    # ... کد صادرات
    return response
export_to_csv.short_description = "📤 صادرات انتخاب شده‌ها به CSV"
```

## 🎨 قالب‌های سفارشی

### 1. قالب اصلی (`base_site.html`)
- هدر سفارشی با لوگو
- دکمه تغییر تم
- پشتیبانی از نایت مود و RTL

### 2. قالب لیست (`change_list.html`)
- جدول بهینه با انیمیشن‌های hover
- فیلترهای جانبی
- صفحه‌بندی زیبا
- انتخاب چندگانه رکوردها

### 3. قالب فرم (`change_form.html`)
- فیلدهای گروه‌بندی شده
- اعتبارسنجی بلادرنگ
- ذخیره خودکار
- ویجت‌های پیشرفته

### 4. قالب صفحه اصلی (`index.html`)
- آمار کلی سیستم
- عملیات سریع
- فعالیت‌های اخیر
- کارت‌های آماری

## 💻 کدهای CSS و JavaScript

### فایل CSS (`custom-admin.css`)
- متغیرهای CSS برای تم‌ها
- استایل‌های RTL
- انیمیشن‌ها و ترانزیشن‌ها
- طراحی ریسپانسیو

### فایل JavaScript (`custom-admin.js`)
- مدیریت تغییر تم
- اعتبارسنجی فرم‌ها
- ذخیره خودکار
- بهبودهای تعاملی

## ⚙️ تنظیمات پیشرفته

### 1. تنظیمات عملکرد
```python
# در admin.py
list_per_page = 25
list_max_show_all = 100
preserve_filters = True
show_full_result_count = True
```

### 2. تنظیمات جستجو
```python
search_fields = ['doc_id', 'change_title', 'requestor_nationalcode__first_name']
autocomplete_fields = ['change_type', 'requestor_nationalcode']
```

### 3. تنظیمات فیلتر
```python
list_filter = [
    StatusFilter, PriorityFilter, TeamFilter, DateRangeFilter,
    'change_type', 'risk_level', 'change_level'
]
```

## 🔧 سفارشی‌سازی بیشتر

### اضافه کردن فیلتر جدید
```python
class CustomFilter(SimpleListFilter):
    title = 'فیلتر سفارشی'
    parameter_name = 'custom'
    
    def lookups(self, request, model_admin):
        return (
            ('value1', 'گزینه 1'),
            ('value2', 'گزینه 2'),
        )
    
    def queryset(self, request, queryset):
        if self.value():
            return queryset.filter(field=self.value())
        return queryset
```

### اضافه کردن اکشن جدید
```python
def custom_action(modeladmin, request, queryset):
    # کد اکشن
    modeladmin.message_user(request, 'عملیات انجام شد', level='SUCCESS')

custom_action.short_description = "عملیات سفارشی"
```

## 📱 طراحی ریسپانسیو

### نقاط شکست
- **Desktop**: بالای 1200px
- **Tablet**: 768px تا 1200px
- **Mobile**: زیر 768px

### ویژگی‌های موبایل
- منوی همبرگری
- دکمه‌های بزرگ‌تر
- جدول‌های قابل اسکرول
- فونت‌های بزرگ‌تر

## ♿ دسترسی‌پذیری

### ویژگی‌های دسترسی
- **Skip Links**: پرش به محتوای اصلی
- **Focus Management**: مدیریت فوکوس صفحه
- **ARIA Labels**: برچسب‌های دسترسی
- **Keyboard Navigation**: ناوبری با کیبورد

### کلیدهای میانبر
- **Ctrl+S**: ذخیره فرم
- **Ctrl+K**: فوکوس جستجو
- **Escape**: پاک کردن جستجو

## 🐛 عیب‌یابی

### مشکلات رایج

#### 1. قالب‌ها نمایش داده نمی‌شوند
```bash
# بررسی مسیر قالب‌ها
python manage.py findstatic admin/base_site.html
```

#### 2. استایل‌ها اعمال نمی‌شوند
```bash
# جمع‌آوری مجدد فایل‌های استاتیک
python manage.py collectstatic --clear
```

#### 3. JavaScript کار نمی‌کند
- بررسی کنسول مرورگر برای خطاها
- اطمینان از بارگذاری فایل JS

## 📈 بهینه‌سازی عملکرد

### نکات بهینه‌سازی
1. **استفاده از select_related**: کاهش کوئری‌های دیتابیس
2. **صفحه‌بندی**: محدود کردن تعداد رکوردها
3. **کش**: استفاده از کش برای داده‌های ثابت
4. **فشرده‌سازی**: فشرده‌سازی فایل‌های CSS و JS

### تنظیمات بهینه
```python
def get_queryset(self, request):
    return super().get_queryset(request).select_related(
        'change_type', 'requestor_nationalcode', 'requestor_team_code'
    ).prefetch_related('requesttask_set')
```

## 📞 پشتیبانی

برای سوالات و مشکلات:
1. بررسی مستندات Django Admin
2. بررسی کدهای موجود در پروژه
3. بررسی کنسول مرورگر برای خطاها

---

## 🎉 نتیجه‌گیری

این پنل ادمین شامل تمام ویژگی‌های مورد نیاز برای مدیریت سیستم درخواست تغییرات است:

✅ **پشتیبانی کامل از نایت مود**  
✅ **پشتیبانی کامل از RTL**  
✅ **فیلترها و جستجوی پیشرفته**  
✅ **ویجت‌های سفارشی**  
✅ **اکشن‌های گروهی**  
✅ **طراحی مدرن و زیبا**  
✅ **طراحی ریسپانسیو**  
✅ **دسترسی‌پذیری**  

پنل ادمین شما آماده استفاده است! 🚀
